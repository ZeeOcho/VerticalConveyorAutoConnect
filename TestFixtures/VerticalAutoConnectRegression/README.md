# Vertical Auto-Connect regression fixture

Copy the current regression fixture blueprint files (`.sbp` and matching
`.sbpcfg` files) into this directory before the first public repository commit.

The expected U/D/T/V/R behavior is documented in `../../TESTING.md`.
